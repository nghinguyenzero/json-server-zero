{
  "name": "json-server-zero",
  "version": "1.0.0",
  "description": "Fake server",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js",
    "start": "npm run generate-data && node main.js",
    "generate-data": "node generate-data.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "NghiNV",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.4",
    "query-string": "^7.0.0",
    "faker": "^5.5.3"
  }
}
